---
import { getEntry } from 'astro:content';
import PersonaTemplate from '../layouts/PersonaTemplate.astro';

const entry = await getEntry('personas', 'hr');
---

<PersonaTemplate title="Secure, UK Cloud Storage for HR Professionals | Bigby.cloud" description='Employee records need careful handling. Encrypted UK cloud storage for HR professionals, GDPR compliant, no scanning or AI training.' persona={entry.data} />
