---
import { getEntry } from 'astro:content';
import PersonaHubTemplate from '../layouts/PersonaHubTemplate.astro';

const entry = await getEntry('persona-hubs', 'personal');
---

<PersonaHubTemplate title="Secure, UK Cloud Storage for Personal Use | Bigby.cloud" description='Personal cloud storage that treats your files as private by default. Encrypted, UK based, no AI training, from £3.99 per month.' hub={entry.data} />
